<?xml version="1.0"?>
<ruleset name="Conductor">
    <rule ref="Tighten"/>

    <!-- Allow multiple class declarations per file in test support files -->
    <rule ref="PSR1.Classes.ClassDeclaration">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>
</ruleset>
